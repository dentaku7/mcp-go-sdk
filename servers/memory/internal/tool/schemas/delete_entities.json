{
	"type": "object",
	"properties": {
		"entityIds": {
			"type": "array",
			"items": {
				"type": "string"
			},
			"description": "An array of entity IDs to delete from the knowledge graph"
		}
	},
	"required": ["entityIds"]
}
